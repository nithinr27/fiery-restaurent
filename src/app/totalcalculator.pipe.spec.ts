import { TotalcalculatorPipe } from './totalcalculator.pipe';

describe('TotalcalculatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalcalculatorPipe();
    expect(pipe).toBeTruthy();
  });
});
